// 2020-10-04 16:32:27
#event properties (no comments/etc. here are saved)
parent_index = oTileFloor;
sprite_index = sprTileFloor;
uses_physics = false;
mask_index = maskNone;

#event other_user0 Update depth & activate
depth = 0;
instance_create(x + TILE_UNIT * 0.5, y + TILE_UNIT * 0.5, oPlayer);
